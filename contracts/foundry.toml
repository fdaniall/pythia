[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200
evm_version = "cancun"

# Initia MiniEVM appchain — update RPC URL after spinning up your rollup
[rpc_endpoints]
initia_testnet = "${INITIA_RPC_URL}"

[etherscan]
initia_testnet = { key = "", url = "" }
